{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null && (data.child('hostUid').val() == auth.uid || newData.child('hostUid').val() == auth.uid || auth.token.role == 'admin' || root.child('admins').child(auth.uid).val() == true)"
      }
    },
    "matches": {
      "$matchId": {
        ".read": "auth != null",
        ".write": "auth != null && (data.child('hostUid').val() == auth.uid || newData.child('hostUid').val() == auth.uid || auth.token.role == 'admin' || root.child('admins').child(auth.uid).val() == true)"
      }
    },
    "presence": {
      "$userId": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid == $userId"
      }
    },
    "liveTournaments": {
      "$tournamentId": {
        ".read": true,
        ".write": "auth != null && (data.child('hostUid').val() == auth.uid || newData.child('hostUid').val() == auth.uid || auth.token.role == 'admin' || root.child('admins').child(auth.uid).val() == true)"
      }
    },
    "live_matches": {
      "$tournamentId": {
        ".read": true,
        ".write": "auth != null && (data.child('hostUid').val() == auth.uid || newData.child('hostUid').val() == auth.uid || auth.token.role == 'admin' || root.child('admins').child(auth.uid).val() == true)"
      }
    }
  }
}
